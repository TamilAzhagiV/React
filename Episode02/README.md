npm- (not node package manager but not stands for npm )--> it just manage the packages (package manager)

npm is the standred repo for all the packages
    all lib and utility manages it.

npm init- > package name-> version-> description->entry point(index.html)-> text command(jest)-> y(git hub repo)-> y--> get package.json file


package.json-> it all config of npm, called as dependies why need-> npm take care of version from package.json